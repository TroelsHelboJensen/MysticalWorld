{
  "name": "mystical-world",
  "version": "0.1.0",
  "description": "A top-down RPG inspired by The Legend of Zelda: A Link to the Past",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test",
    "start": "npx serve . --listen 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "jimp": "^1.6.0",
    "jsdom": "^24.0.0",
    "vitest": "^1.4.0"
  }
}
